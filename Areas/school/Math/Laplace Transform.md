- Used to transfer from time domain to the s domain
- notation $\mathscr{L}(f(t)) \to F(S)$
- transform takes you from one set of functions to another set of functions.
- $\mathscr L(f(t)) =\int_{∞}  \, dx$

---
# Resources 